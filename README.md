# AI Code Assistant (IntelliJ Plugin)

Simple IntelliJ IDEA plugin that uses an LLM to explain selected code directly inside the IDE.

## What it does

- Explains selected code in the editor using AI
- Uses IDE context (selected code + surrounding code + file name)
- Displays AI response in a popup inside IntelliJ
